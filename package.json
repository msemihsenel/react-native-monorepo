{
  "name": "react-native-monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist":[]
  },
  "scripts": {
    "clear-node_modules": "rm -rf node_modules && rm yarn.lock && cd packages/mobile && rm -rf node_modules && cd ../web && rm -rf node_modules && cd ../.."
  }
}
